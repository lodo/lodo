<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>LODO</title>
  <%= stylesheet_link_tag 'journals' %>
  <%= stylesheet_link_tag 'lodo' %>
  <%= javascript_include_tag 'jquery' %>
  <% Dir.open("#{RAILS_ROOT}/public/javascripts/").reject {|f| f.to_s =~ /jquery/ }.reject{|f| f.to_s =~ /^\.+$/}.each do |js| -%>
    <script src="/javascripts/<%= js.to_s -%><%= ActionController::Base.perform_caching ? "" : "?#{(rand() * 10000000000000000).to_i}" -%>" type="text/javascript"></script>
  <% end %>
</head>
<body>
  <% if @me %>
  
  <div class='main_menu'>
    <ul>
      <li>
	<%= link_to t(:user_administration, :scope => :layout), users_path %>
      </li>

      <li>
	<%= link_to t(:chart_of_accounts, :scope => :layout), accounts_path %>
      </li>
      <li>
      <%= link_to t(:vat_plan, :scope => :layout), vat_accounts_path %>
      </li>

      <li>
	<%= link_to t(:journals, :scope => :layout), journals_path %>
      </li>

      <li>
	<%= link_to t(:companies, :scope => :layout), companies_path %>
      </li>

      <li>
	<%= link_to t(:projects, :scope => :layout), projects_path %>
      </li>

      <li>
	<%= link_to t(:units, :scope => :layout), units_path %>
      </li>

      <li>
	<%= link_to t(:products, :scope => :layout), products_path %>
      </li>

      <li>
	<%= link_to t(:orders, :scope => :layout), orders_path %>
      </li>

      <li>
	<%= link_to t(:bills, :scope => :layout), bills_path %>
      </li>

      <li>
	<%= t(:logged_in_as, :scope => :layout) -%> <em><%= link_to @me.login, :controller => 'users', :action => 'show', :id => @me.id %></em> (<%= link_to t(:log_out, :scope => :layout), :controller => 'users', :action => 'logout' %>)
      </li>

      <li class="change_company_form">
        <% form_tag :controller => "users", :action=> "change_current_company" do -%>
         <label for="current_company"><%= t(:active_company, :scope => :layout) -%>:</label>
         <%= select_tag "current_company", options_for_select(@me.companies.collect {|p| [ p.name, p.id ] },
                                                              if @me.current_company.nil? then 0 else @me.current_company.id end ) %>
        <% end %>
        <script>submitAction.registerSubmitAction('#current_company', 'change', '.change_company_form form');</script>
      </li>

    </ul>
  </div>

  <% end %>

  <% if flash[:notice] %>
  <div class='messages'>
    <div class='messages_inner'>
      <%= flash[:notice] %>
    </div>
  </div>
  <% end %>

  <div class='content'>
    <div class='content_inner'>
      <%= yield  %>
      <div class='content_post'>
      </div>
    </div>
  </div>

  <script>
    stripe();
  </script>
</body>
</html>
